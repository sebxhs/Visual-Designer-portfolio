# Sebastian Portfolio

A responsive static portfolio site for Sebastian, an independent visual designer. The site includes selected work, services, about, and contact sections with lightweight JavaScript for navigation, filtering, and scroll reveal effects.

## Preview locally

From the project directory, run:

```bash
python3 -m http.server 8080
```

Then open <http://localhost:8080>.
